ssh-keygen -t rsa -b 4096 -C "danielmognato@hotmail.com"

cat ~/.ssh/id_rsa.pub

depois adicionar a hash no github

depois no git colocar:
eval "$(ssh-agent -s)"

ssh-add ~/.ssh/id_rsa




https://docs.github.com/pt/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent